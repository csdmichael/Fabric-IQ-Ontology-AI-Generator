{
  "name": "fabric-iq-ontology-ai-generator",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "ui",
    "api"
  ],
  "scripts": {
    "start": "concurrently \"npm run start --workspace ui\" \"npm run dev --workspace api\"",
    "start:ui": "npm run start --workspace ui",
    "start:api": "npm run dev --workspace api",
    "build": "npm run build --workspace ui && npm run build --workspace api",
    "test": "npm run test --workspace ui && npm run test --workspace api",
    "lint": "npm run lint --workspace ui"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
